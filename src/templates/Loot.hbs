<div class="card front {{{ cardType }}}">
  <header>
    <div class="title">{{{ Title }}}</div>
  </header>
  <article>
    <div class="type mdl-badge-left" data-badge="1">{{{ Type }}}</div>
    <div class="effect mdl-badge-left" data-badge="2">{{{ Effect }}}</div>
    <div class="description">{{{ Description }}}</div>
  </article>
  {{> footer }}
</div>