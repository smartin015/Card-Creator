<div class="card front {{{ cardType }}} {{{ Class }}}">
  <header>
    <div class="title">{{{ Title }}}</div>
  </header>
  <article>
    <div class="effect"><strong>Basic attack:</strong> {{{ Attack }}}</div>
    <div class="description">{{{ Description }}}</div>
  </article>
  {{> footer }}
</div>