<div class="card front {{{ cardType }}}">
  <header>
    <div class="indicators">
      <div class="health mdl-badge" data-badge="2">{{{ Health }}}{{> icon img='hp' }}</div>
    </div>
    <div class="info">{{{ Title }}}</div>
  </header>
  <article>
    <div class="skills mdl-badge-left" data-badge="3"><strong>Skills:</strong> {{{ Skills }}}</div>
  </article>
  {{> footer }}
</div>