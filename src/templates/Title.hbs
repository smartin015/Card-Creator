<div class="card front {{{ cardType }}} horizontal">
  <header>
    <div class="name mdl-badge-left" data-badge="1">{{{ Name }}}</div>
  </header>
  <article>
    <div class="passive mdl-badge" data-badge="2"><strong>+5 to non-ability rolls involving:</strong><br/>{{{ passivebonustext }}}</div>
    <br/>
    <div class="active mdl-badge" data-badge="3"><strong>Once per Encounter:</strong><br/>{{{ onceperencounter }}}</div>
  </article>
  <footer>
    <div class="flavortext">{{{ Description }}}</div>
    <div class="cardType">{{ cardType }}</div>
  </footer>
</div>